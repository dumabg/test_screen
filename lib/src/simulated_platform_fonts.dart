/// Simulated fonts for the different target platforms
enum SimulatedPlatformFonts {
  /// Android Roboto
  roboto,

  /// iOS .SF Pro Display
  sfProDisplay,

  /// iOS .SF Pro Text
  sfProText,

  /// iOS .SF UI Display. Currently use the same font as [sfProDisplay].
  sfUIDisplay,

  /// iOS .SF UI Text. Currently use the same font as [sfProText].
  sfUIText,

  /// Emojis
  notoColorEmoji
}
